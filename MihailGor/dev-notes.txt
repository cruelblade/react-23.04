(ур. 2)Pure component используется для отмены ререндера используемых в родительском компоненте компонентов
(ур. 2)e.target.dataset.count - находит атрибут в существующем DOM элементе перед которым стоит data, пример data-count
(ур. 3)clean-webpack-plugin для чистки хэша
(ур. 3)про оптимизацию и merge-plugin почитать в webpack.js.org(про отдельные конфиги для боя и теста)
sass loader почитать как подключить и что делает
purge css - избавление от лишнего css, но может вырезать лишнее
1:03:29 про разбиение css в разные конфиги для теста и боя
1.07.18 - обнуляемся